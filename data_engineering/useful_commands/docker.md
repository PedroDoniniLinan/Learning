## Compose

command | descripion
--|--
`docker compose up` | start container (inititate it if first time)
`docker compose down` | stop container
`docker compose down -v` | stop container and delete volume